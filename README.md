# TOP1 TOPUP Pro Demo

Front-end demo only. No real payment or Free Fire diamond delivery is connected.

Upload `index.html`, `style.css`, `script.js`, and `README.md` to the repository root. Keep GitHub Pages on `main` + `/(root)`.
