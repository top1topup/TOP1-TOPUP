const packages=[['25 💎','৳20'],['50 💎','৳35'],['115 💎','৳75'],['240 💎','৳145'],['610 💎','৳350'],['1240 💎','৳690']];
let selected=null;
const box=document.getElementById('packages');
packages.forEach((p,i)=>{const el=document.createElement('button');el.className='pkg';el.innerHTML=`<b>${p[0]}</b><small>${p[1]}</small>`;el.onclick=()=>{document.querySelectorAll('.pkg').forEach(x=>x.classList.remove('active'));el.classList.add('active');selected={i,diamonds:p[0],price:p[1]};document.getElementById('selectedText').textContent=`${p[0]} — ${p[1]}`};box.appendChild(el)});
function render(){const data=JSON.parse(localStorage.getItem('top1_orders')||'[]');const list=document.getElementById('orderList');if(!data.length){list.innerHTML='<p class="muted">No orders yet.</p>';return}list.innerHTML=data.map(o=>`<div class="order"><div><b>${o.id}</b><div class="muted">Player ID: ${o.playerId} • ${o.diamonds} • ${o.price}</div></div><span class="status">Pending</span></div>`).join('')}
document.getElementById('buyBtn').onclick=()=>{const playerId=document.getElementById('playerId').value.trim();if(!playerId)return alert('Please enter Player ID.');if(!selected)return alert('Please choose a package.');const orders=JSON.parse(localStorage.getItem('top1_orders')||'[]');const o={id:'T1-'+Date.now().toString().slice(-8),playerId,diamonds:selected.diamonds,price:selected.price};orders.unshift(o);localStorage.setItem('top1_orders',JSON.stringify(orders));render();alert(`Demo order created: ${o.id}`);location.hash='orders'};document.getElementById('year').textContent=new Date().getFullYear();render();