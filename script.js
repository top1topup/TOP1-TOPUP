let selected=null;const packs=document.querySelectorAll('.pack');const selectedEl=document.getElementById('selected');const totalEl=document.getElementById('total');const list=document.getElementById('orderList');packs.forEach(p=>p.addEventListener('click',()=>{packs.forEach(x=>x.classList.remove('selected'));p.classList.add('selected');selected={diamonds:p.dataset.diamonds,price:p.dataset.price};selectedEl.textContent=selected.diamonds+' 💎';totalEl.textContent='৳'+selected.price}));function render(){const orders=JSON.parse(localStorage.getItem('top1topup_orders')||'[]');if(!orders.length)return;list.innerHTML=orders.map(o=>`<div class="order"><strong>${o.id}</strong> — ${o.diamonds} 💎 — ৳${o.price}<br><small>Player ID: ${o.playerId} • Status: Pending (Demo)</small></div>`).join('')}document.getElementById('orderBtn').addEventListener('click',()=>{const playerId=document.getElementById('playerId').value.trim();if(!playerId)return alert('Please enter your Player ID.');if(!selected)return alert('Please select a diamond package.');const order={id:'T1-'+Date.now().toString().slice(-7),playerId,diamonds:selected.diamonds,price:selected.price};const orders=JSON.parse(localStorage.getItem('top1topup_orders')||'[]');orders.unshift(order);localStorage.setItem('top1topup_orders',JSON.stringify(orders));alert('Demo order created: '+order.id);render()});render();